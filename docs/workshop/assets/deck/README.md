# Deck stills and clips

Copied from `SLM-RL-Presentation/public/assets` so Colab can load them from
this repo (`raw.githubusercontent.com/.../docs/workshop/assets/deck`).

Refresh from the sibling presentation tree:

```bash
python docs/workshop/import_deck_assets.py
```

Workshop pipeline SVGs stay in `../diagrams`. They are rendered here and
synced *to* the deck, not imported from it.
