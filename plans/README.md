# Implementation Plans

Generated by the improve skill on 2026-07-11 (advisor session: RL-pipeline
consolidation — SLM-RL vs Karpathy policy-gradients / atari-rl replay /
Gymnasium vectorization). Planned at commit `cd2b4f4`. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

## The v2 pipeline these plans build

```
STAGE 0 (once per run): teacher plays WITH rationales (002) + strategy prompt (002)
   → SFT LoRA base → adopted unconditionally as RL init (001)
STAGE 1..N: GRPO/reject_sft from that base
   → replay window over accumulated rollouts (004)
   → return-weighted SFT pairs + 2 GRPO epochs (003)
   → batched collection for wall-clock (005)
CURRICULUM: start on mastermind-easy, 64 codes (006), ramp to standard
```

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Adopt teacher warm-start unconditionally as RL init | P1 | S | — | DONE |
| 002 | Reasoning teacher (rationales) + strategy prompt | P1 | M | — (001 recommended first) | DONE |
| 006 | mastermind-easy curriculum entry point | P1 | S | — | DONE |
| 004 | Cross-generation replay window | P1 | M | 001 | DONE |
| 003 | Return-weighted SFT + 2 GRPO epochs | P2 | S | 002 | DONE |
| 007 | Live-play web UI (stream episodes to browser) | P2 | M | — (payoff bigger after 002) | DONE |
| 005 | Batched episode collection | P3 | L | — (land last) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 before 004: both edit `run_generation` in `slm_rl/orchestrator/generation.py`
  (gate section vs dataset section) — sequencing avoids conflicts.
- 002 before 003: both edit `slm_rl/datagen/sft_export.py`.
- 005 last: it is a wall-clock multiplier with the widest blast radius
  (runner semantics); every other plan is independent of it.
- 007 (added 2026-07-11 at user request) is a pure observer (stdlib web UI
  tailing rollout JSONLs); independent of all training plans, best landed
  after 002 so rationales stream. Executors must read `CODING_GUIDELINE.md`
  (repo root) before starting any plan.
- After 001+002+006 land, the intended first experiment is:
  `slm-rl evolve --game mastermind-easy --train-strategy reject_sft --warm-start --pruner --generations 3`
  on LFM2.5-350M, then the same on standard mastermind with the 1.2B + grpo.

## Findings considered and rejected

- **Graft a discounted terminal reward into GRPO's game_ctx**: no-op — the
  historical episode outcome is constant across a prompt's sample group, and
  GRPO's group-normalized advantage cancels per-prompt constants exactly.
  Cross-turn credit is already carried by the potential-shaped elimination
  reward. Documented in plan 003 (docs/HYBRID_RL.md paragraph).
- **Further gate tuning (margin below 0.01)**: the gate is not the bottleneck
  after plan 001; margins below ~2σ promote noise.
- **More GRPO on the raw 350M at standard difficulty**: measured dead end
  (July 2026: flat 0%, one entropy collapse); revisit only after 002+006.
- **Counting pruner-assisted wins as model skill**: permanently rejected —
  eval-gate purity is a design invariant (D11, HYBRID_RL.md); `eval_pruned`
  stays a recorded-only product metric.
- **Adopting OpenRL/Gymnasium as a dependency**: the Game ABC already mirrors
  the Gymnasium contract (D1); `bridges/gym_adapter.py` remains the planned
  integration point for external envs. Patterns were adopted (vectorization,
  replay), not the frameworks.

## Batch 2 (2026-07-11, user request: Space Invaders via Gymnasium)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 008 | Space Invaders via Gymnasium/ALE (RAM -> text) | P1 | L | — | DONE |
| 009 | Heuristic Space Invaders teacher + warm start | P1 | M | 008 | PLANNED-NEXT (not yet written) |
